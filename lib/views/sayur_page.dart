import 'package:flutter/material.dart';

class SayurPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    // Isi halaman detail produk di sini
    return Scaffold(
      appBar: AppBar(
        title: Text('Detail Produk'),
      ),
      body: Center(
        child: Text('Halaman Detail Produk'),
      ),
    );
  }
}